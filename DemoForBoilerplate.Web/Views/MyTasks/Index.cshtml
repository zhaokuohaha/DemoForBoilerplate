@using Abp.Web.Mvc.Extensions;
@model DemoForBoilerplate.MyTasks.Dtos.GetMyTaskOutput
@{
	ViewBag.Title = "Tasks";
}
@section scripts{
	@Html.IncludeScript("~/Views/MyTasks/Index.js")
}

<div class="row">
	<div class="col-md-12">
		<div class="panel panel-primary">
			<div class="panel-heading">
				<div class="panel-title">
					<h2>@L("Task Management")</h2>
				</div>
			</div>
			<div class="panel-body">
				<table class="table table-bordered">
					<tr>
						<th>@L("Task Id")</th>
						<th>@L("Person")</th>
						<th>@L("Task Description")</th>
						<th>@L("Creation Time")</th>
						<th>@L("Task State")</th>
						<th>@L("Operation")</th>
					</tr>
					@foreach (var item in Model.Tasks)
					{
						<tr>
							<td>@item.Id</td>
							<td>@item.AssignedPersonName </td>
							<td>@item.Description</td>
							<td>@item.CreationTime</td>
							<td>@item.State</td>
							<td>
								<div class="button-group">
									@Ajax.ActionLink(L("Eidt"), "EditTask", new {id = item.Id }, new AjaxOptions { InsertionMode = InsertionMode.Replace, UpdateTargetId = "ajaxmodal", OnComplete = "showModal" }, new { @class = "btn btn-primary" })
									@*@Ajax.ActionLink(L("Delete"),"DeleteTask","",new AjaxOptions { InsertionMode = InsertionMode.Replace, UpdateTargetId="ajaxmodel"}, new { @class = "btn btn-danger" })*@
									<button class="btn btn-danger" data-itemid="@item.Id">@L("Delete")</button>
								</div>
							</td>
						</tr>
					}
				</table>
			</div>
		</div>
	</div>
	<div class="col-md-12">
		@Ajax.ActionLink("CreateNewTask", "CreateMyTask",new { controller="MyTasks"}, new AjaxOptions { UpdateTargetId = "ajaxmodal", InsertionMode = InsertionMode.Replace,HttpMethod = "get",OnComplete="showModal"}, new { type="button", @class="btn btn-primary"})
	</div>
</div>
<div class="row">
	<div class="col-md-12">
		<div class="modal fade" id="TaskCreateModal" tabindex="-1" role="dialog" aria-labelledby="TaskCreateModalLabel" data-backdrop="static">
			<div class="modal-dialog" role="document">
				<div class="modal-content">
					<form name="taskCreateForm" id="ajaxmodal" role="form" novalidate class="form-validation">
						
					</form>
				</div>
			</div>
		</div>
	</div>
</div>
<script>
	function showModal() {
		$('#TaskCreateModal').modal();
	}
	
</script>
